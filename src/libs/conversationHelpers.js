export const createUser = async (id, username) => {
  // make api request with the id and username to see if user exist
  // if user exist return user
  // else create user and return username and id
};

export const conversationExist = async () => {};
